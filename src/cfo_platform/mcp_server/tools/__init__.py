"""MCP tool modules register themselves here as they're built, e.g.:

    data_tools.py      -- query a client's warehouse (list_clients, run_sql, ...)
    analytics_tools.py -- run a registered Analyzer and return its result
    reporting_tools.py -- run a registered ReportBuilder and return the file path

None exist yet — see docs/architecture/overview.md.
"""
